Core Functionalities

Merge PDFs

Split PDF (by pages, ranges, or extract specific pages)

Compress PDF (low, medium, high)

Add watermark (text or image)

Rotate pages (90°, 180°, custom)

Reorder pages (drag & drop order)

Delete pages

Extract text

Extract images

Convert images → PDF

Convert PDF → images

PDF metadata editor (title, author, subject, keywords)

Password protect PDF

Remove password (if user provides the correct password)

Optimize PDF (remove unused objects, clean structure)

✳️ Advanced Functionalities

Fillable form detection

Edit form fields

Flatten form fields

Combine multiple PDF operations into a single workflow

Page cropping

Page resizing

Add page numbers

Add header/footer text

Dark/light theme UI

Save recent operations (local storage)

Local file history with thumbnails

🚀 Nice-to-Have / Later Features

Drag & drop full editor workspace (like Adobe online)

PDF compare (diff two PDFs)

Sign PDF (draw or upload signature)

Annotate PDF (highlight, underline, notes)